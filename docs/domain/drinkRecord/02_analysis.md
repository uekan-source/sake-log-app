# お酒の記録 — 思考過程（メモ・提出しない）

この文書は書き換えない。段階が進むたびに追記する（目次だけは追記のたびに更新する）。

## 目次

**経緯・要求まわり**

- [題材決定の経緯](#theme)
- [要求文からの仕分け（受注側の整理）](#sorting)
- [ボスの指摘とその検証](#boss-review)
- [進め方の変更（質問リストの分担）](#process-change)
- [受け取った要求の原文アーカイブ（01 から移設）](#original-text)
- [質問番号の振り直し（新旧対応表）](#q-renumber)

**設計の材料**

- [論点の種（設計段階で扱う）](#seeds)
- [アクター洗い出し（段階 1）── 7 つの問い／6 類型／所持物／はみ出し](#actors)
- [業務フローの棚卸し（段階 3）── 5 軸チェック／振り分けの判断メモ](#flow-inventory)
- [名詞と動詞（段階 4）](#nouns-verbs)
- [名詞のふるい分け（段階 5）](#sifting)
- [ユビキタス言語（段階 6）── 候補・7 軸評価・呼ばない語](#naming)
- [概念を配る（段階 7）── コンテキスト判断・配る表・テーブル名](#contexts)
- [段階 7 の修正 ── コンテキストは `user` 1 つ（ユーザーの反証）](#contexts-fix)
- [モデルの形（段階 8）── Brand / DrinkRecord の case class と決めごと](#model-shape)
- [流す（段階 9）── フローの通し・穴 4 つ・逆算チェック](#verification)
- [出す前のチェック ── 用語統一と手入力銘柄の初期状態](#final-check)
- [実現方式のメモ（設計の外）](#impl-notes)

**質問リストの経過（当時の番号のまま。新旧対応表を参照）**

- [Q1〜Q8 の回答で分かったこと](#qa-1-8)
- [Q9・Q10 の回答で質問リスト一巡完了](#qa-9-10)
- [Q11〜Q14 の回答と仕様変更 3 件](#qa-11-14)

<a name="theme"></a>

## 題材決定の経緯（2026-08-20）

- ポートフォリオの条件：本当に自分が使うもの。同期の題材（チームのランチ店探し）と被らないこと
- 却下した候補と理由：先輩への質問管理（口頭で足りるため使われない）／社内本の貸出（動きが薄い）／彼女との共有アプリ（遠距離・月 1 で頻度不足。彼女はアクターでなく「同席者」のデータにする）
- 採用の根拠：ラベル写真を既に撮っている／撮ったまま埋もれている／月 1 でも残したい──要求が全部実話で、発明した要求がゼロ

<a name="sorting"></a>

## 要求文からの仕分け（受注側の整理）

殴り書きに混ざっていた実現方法を要求から分離した。

| 原文 | 仕分け | 発注者の言葉に翻訳すると |
|---|---|---|
| 「enum で管理すればいい」 | 設計判断 | 種類（ワイン・日本酒など）が増えても受けられるようにしたい |
| 「検索するだけならコストはさほど変わらない」 | 実現方式の見積もり | ──（要求ではない） |

<a name="boss-review"></a>

## ボスの指摘とその検証（2026-08-20）

**指摘：** プロンプトが大きくなるから、お酒の種類は絞った方がいい。

**検証：** プロンプトは種類数に比例して育つが、3 種で千〜二千トークン程度と誤差。さらに「種類判定 → 種類別の抽出」の 2 段構成にすれば種類が増えてもプロンプトは一定（区分値が隣のフィールドの読み方を決める、の AI 呼び出し版）。**絞るべき本当の理由はプロンプトではなく設計量**（種類ごとに属性・確認画面・データ例・検証が増える）。

**採用：** 深く属性を持つ種類はワインと日本酒の 2 種に絞る（発注者の判断。よく写真を撮るのがこの 2 種のため）。ウイスキーは今回やらない → 2 周目の追加機能候補。種類を後から 1 つ足して設計が壊れなければ、切り分けが正しかった検証になる。

<a name="seeds"></a>

## 論点の種（設計段階で扱う。まだ結論を出さない）

- **種類は enum、品種・酒米はテーブルか文字列。** `design_lessons.md`「enum にするかテーブルにするか」の基準で確認済み：種類は増えたら属性と画面のコードを書く（エンジニアが増やす）→ enum。品種は増えてもコードが変わらず、AI とユーザーが増やす開集合 → enum 禁止。品種をマスタにすると表記ゆれ（ピノノワール／ピノ・ノワール）を引き受ける → 付録B 候補
- **AI の推定値とユーザーの確定値の区別。** 写真 → 解析中 → 推定（未確定）→ 確認・修正 → 確定、の状態を持つか。AI が外れても手入力に落ちて成立する構造（カバレッジの穴で死なない）
- **銘柄（マスタ）と「飲んだ記録」（体験）の分離。** 同じ銘柄を別の日に飲む。ワインはさらにヴィンテージ（同じ銘柄の別の年）をどこに置くかがある
- **「調べただけ」と「飲んだ記録」の分離。** メニューで照会して頼まなかった酒を保存するか
- **AI は外部サービスのアクター。** 所持物なし、結果を写す列が要る（レセコンと同じ型）
- **ログインの意味づけ。** 複数ユーザー前提にするなら userId が全テーブルに乗る境界の鍵になる（shopId の教訓）。ただし発注者は「どちらでも」なので、意味の持たせ方から設計判断
- **目的が「貯める」より「覚える・詳しくなる」に寄っている。** ただ保存できても見返さなければゴールに届かない。振り返りの形は要求にまだ無い → 質問リスト候補の領域

<a name="process-change"></a>

## 進め方の変更（2026-08-20）

質問リストは本来ユーザー自身が書くルール（研修で一番手戻りが出た工程のため）だが、**今回はユーザーの指示で Claude が起案し、発注者＝ユーザーが回答する形**に変更。起案は「設計案を提示して合意を取る」書き方（はい／いいえで答えられる形）にし、判断は発注者に残す。

<a name="actors"></a>

## アクター洗い出し（段階 1・Claude 起案。ユーザーのレビューで確定）

### 7 つの問い

| # | 問い | 答え |
|---|---|---|
| 1 | このデータを最初に作るのは誰か | 記録者（自分）。ただし銘柄情報の**下書き**は外部の知識源（AI）が作る |
| 2 | 日常的に変更するのは誰か | 記録者（推定の修正・確定、感想の追記） |
| 3 | 止める・承認する権限を持つのは誰か | 記録者自身（推定を「確定」する操作が承認に相当）。第三者の承認者はいない |
| 4 | お金を受け取る／払うのは誰か | いない（無料の個人アプリ。決済なし。API 利用料は運用の話でアクターではない） |
| 5 | 集計を見るのは誰か | 記録者（自分の記録の検索・振り返り）。経営・分析はいない |
| 6 | 問い合わせを受けるのは誰か | いない（個人アプリ） |
| 7 | 人ではないが動くものは何か | 外部の知識源（AI・検索 API）。過去写真の一括取り込み処理（非同期にするなら） |

### 6 類型の確認

| 類型 | いる／いない | 理由 |
|---|---|---|
| 運営者・本部 | いない | 利用者＝運営者が同一人物。複数ユーザー化しても「他人のデータを管理する人」は置かない想定（→ 質問 Q8 に連動） |
| 承認者・監査 | いない | 確定操作は記録者自身が行う |
| CS・サポート | いない | 個人アプリ |
| 経営・分析 | いない | 振り返りは記録者自身の機能 |
| バッチ・スケジューラ | **未定** | 過去写真の一括取り込みを非同期処理にするか次第（→ Q6）。定期処理は今のところ要求に無い |
| 外部サービス | いる | 知識源（AI・検索 API）。**所持物なし。推定結果を写す列が要る**（レセコンと同じ型） |

### 所持物

| アクター | 触るもの | 所持物 | 変更の頻度 |
|---|---|---|---|
| 記録者（自分） | 飲んだ記録・ラベル写真・自分の感想 | 飲んだ記録、写真、感想・評価、確定した銘柄情報 | 月 1 回程度＋取り込み時 |
| 外部の知識源（AI） | 銘柄情報の推定 | なし（結果を返すだけ） | 照会のたび |
| 同席者（彼女など） | ── | ──（アクターではなく記録の属性） | ── |

### はみ出し

| チェック | 結果 | 対応 |
|---|---|---|
| 2 人が同じものを対等に変更する | **あり**：銘柄情報を「AI が下書きし、記録者が直す」 | 対等ではなく段階が違う（推定 → 確定）。論点の種「推定値と確定値の区別」で扱う |
| 所持物が極端に多いアクター | 記録者に集中するが、1 人用アプリなので自然 | 対応なし |
| 5 分以上迷ったもの | 過去写真の取り込みは「機能」か「初期データ移行」か | Q6 の回答を見て段階 3（フロー）で決める |

<a name="qa-1-8"></a>

## Q1〜Q8 の回答で分かったこと（2026-08-20 追記）

- **Q1 の起案は外れていた。** 「AI の説明 vs 自分の感想」の区別を提案したが、発注者の意図は「**味わい**（商品ごとの個体差。同じメルローでも軽い・重い）＋**豆知識**（覚える助けになる背景話）」だった。どちらも AI が調べる側の情報。**自分の感想を持つかは未確定のまま残った → Q10 を追加**。外した理由：目的（覚える・詳しくなる）より先に項目の形を聞いた
- **「誰と」は記録しない（Q2）。** アクターシートに置いた「同席者（彼女など）＝記録の属性」は**属性としても消えた**。段階 1 の所持物表のその行は撤回
- **写真の取り込みは一度きりの初期移行ではなく継続機能（Q6）。** 「撮ったのにアップロードしなかった写真を後からあげる」が通常の使い方に含まれる。はみ出し表で 5 分迷った「機能か移行か」は**機能**で確定。まとめて取り込み → 後から順に確認、なので**未確認の推定が溜まっている状態**が普通に存在する → 状態設計に効く
- **応答時間の要求が出た（Q4）。** 利用イメージは「注文 → ボトルの写真を撮る → **飲みながら画面をチェック**」。推定結果は飲んでいる間（数分以内）に見たい。確認・修正は後でよい。段階 3 のフローはこの場面を軸に書く
- 「気になる」（調べたが飲んでいない）は残す（Q3）。論点の種「調べただけと飲んだ記録の分離」は要求として確定
- 残り：**Q9（保存期間）未回答、Q10（自分の感想）新規**

<a name="qa-9-10"></a>

## Q9・Q10 の回答で質問リスト一巡完了（2026-08-20 追記）

- 保存は無期限・自動削除なし・個別削除のみ（Q9）。定期削除バッチは立てない → 6 類型の「バッチ・スケジューラ」は取り込みの非同期処理だけが残る
- **感想の専用欄は持たない（Q10）。** 感想があれば味わい・豆知識の欄に追記する。→ **論点の種に追加**：確定後の味わい・豆知識は「AI の下書き＋人の追記」が混ざった本文になる。**AI で再取得（再推定）すると人の追記が消える**危険がある。再取得を許すか、許すなら追記をどう守るかは「型では守れない決めごと」候補

<a name="flow-inventory"></a>

## 業務フローの棚卸し（段階 3・Claude 起案。番号なし・振り分け前）

- [記録者] アカウントを登録する
- [記録者] ログインする
- [記録者] 店で酒を注文し、ボトルが来たらラベルの写真を撮って登録する
- [記録者] メニューにある酒の名前・品種・特徴を入力して調べる
- [記録者] 飲みながら、表示された銘柄情報の画面を見る
- [記録者] 調べた酒を「飲んだ記録」として保存する
- [記録者] 調べたが飲まなかった酒を「気になる」として残す
- [記録者] 推定の間違いを直して確定する
- [記録者] 感想があれば味わい・豆知識の欄に追記する
- [記録者] 知識源が読み取れなかった酒を手入力で登録する
- [記録者] 撮ったのにあげていなかった写真を後日まとめてアップロードする
- [記録者] 溜まった未確定の記録を後から順に確認・確定する
- [記録者] 品種・産地・味わいで自分の記録を検索して振り返る
- [記録者] 不要になった記録・写真を個別に削除する
- [システム] 登録された写真・入力テキストを知識源に照会する
- [システム] 返ってきた銘柄情報を「未確定」として記録に書き込み、表示する
- [システム] 読み取れなかったときはその旨を示し、手入力に切り替える
- [知識源] 写真・テキストから銘柄情報（名前・種類・品種・産地・味わい・豆知識）を推定して返す
- [-] 自分の記録は自分にしか見えない
- [-] 深く扱うのはワインと日本酒。その他は浅く受ける
- [-] 写真の無い記録が存在してよい（メニューから調べて保存した場合）
- [-] 同じ銘柄を別の日に飲んだら記録は 2 件。銘柄情報は共通
- [-] 保存は無期限。自動削除はしない
- [-] 確定後の再推定は人の追記を消す恐れ（扱い未定）

### 5 軸チェックで出た取りこぼし（→ 質問 Q11〜Q14 を追加）

| 軸 | 見つかったもの | 行き先 |
|---|---|---|
| 打ち消し | 「気になる」の酒を後日実際に飲んだら？（気になる → 飲んだ、への昇格。終端の無い状態を作らないため） | Q11 |
| 時間 | 未確定のまま放置された記録はどうなる？（期限・催促・検索に出るか。処方箋演習の「放置に終端が無い」と同じ形） | Q12 |
| 打ち消し | 確定は取り消せるか。確定後の編集・AI への再照会はできるか | Q13 |
| 数 | 1 枚の写真に複数本のボトルが写っていたら？ | Q14 |
| 同時 | 同じ写真を 2 回あげたら重複するが、記録 2 件は業務として正常（Q2）なので例外にしない | ──（対応なし） |
| ゼロ | 記録 0 件・検索 0 件は表示の話で業務ルール不要。写真 0 枚の記録は例外欄に明記した | 例外欄 |

### 振り分けの判断メモ

- 本文は正常系で通し、分岐（読み取れない・飲まなかった）も**取引がどう終わるかの分岐**なので本文に残した。行数が少なく、外すほど本文が重くない
- 「未確定として書き込む」は実装ではなく業務ルール（発注者に言って通じる：「AI の答えは仮置きで、あなたが確定するまで正にならない」）
- バッチは立てない。まとめ取り込みの照会は [システム] の照会と同じ行為なので、主語を増やさない

<a name="qa-11-14"></a>

## Q11〜Q14 の回答と仕様変更 3 件（2026-08-20 追記）

- Q11・Q12・Q14 は起案どおり確定
- **Q13 で発注者が方針転換。Q10 の回答が覆った**（手戻りとして記録）：感想の専用欄「**備考**」を新設し、AI が調べた欄と人の欄を**完全に分離**する。その結果、**AI の再照会が安全になった**（AI の欄は上書きしてよい。感想は備考にあるので消えない）。AI の欄自体も編集可能（AI が間違うため）。論点の種「再推定で追記が消える」は**この分離で解消**。ただし新しい含意が残る：**再照会は記録者の手修正も上書きする**（発注者は「問題ない」と明言）
- **追加要望①：ログインなし利用。** 調べるのはログイン不要、保存はログイン必須。→ **新アクター「ゲスト（未ログイン）」を段階 1 のシートに追加**（所持物なし。調べて見るだけ）。7 つの問いへの影響：ゲストは何も作らない・変えないので問 1〜6 は変化なし
- **追加要望②：画像の取得。** メニューから調べた場合、知識源が銘柄の画像も探して返す（見つからなければ空欄可）。自分でアップロードも可能。→ **画像の出所が 3 通り**（自分が撮った／知識源が取得／なし）になり、出所の区別が属性候補
- 検算（5 軸・打ち消し）で新しい穴：**ゲストで調べた直後にログインしたら、結果を引き継いで保存できるか** → **Q15 ★ 追加**
- **Q15 確定：引き継げるようにする。** 含意：ゲストの照会結果は「保存されない」が「ログインまでの間だけ生きている」必要がある。**一時的な置き場**という概念が生まれた──エンティティにするか、セッションの持ち物で済むかは段階 5 のふるい分けで扱う

<a name="q-renumber"></a>

## 質問番号の振り直し（2026-08-20）

読みやすさのため、01 の質問番号を**上から表示順**に振り直した。**この文書（02）の本文は当時の番号（旧番号）のまま**。対応は次のとおり。Q1〜Q5 は変更なし。

| 旧 | Q6 | Q7 | Q8 | Q9 | Q10 | Q11 | Q12 | Q13 | Q14 | Q15 |
|---|---|---|---|---|---|---|---|---|---|---|
| 新 | Q12 | Q13 | Q14 | Q15 | Q6 | Q7 | Q8 | Q9 | Q10 | Q11 |

<a name="original-text"></a>

## 受け取った要求の原文アーカイブ（01 から移設。書き換えない）

発注者の指示で 01 は要旨のみとし、原文の引用はここへ移した。

2026-08-20 の殴り書き：

> 一度飲んだワインとか日本酒とかの様々なお酒をその場で調べて保存しときたい。よくお店で飲みに行く時、ボトルの銘柄が置かれて写真を取るが、その場では美味しいと思う一方、すぐに忘れてしまい、自分の知識とかにもなっていない。飲んだお酒の特徴を覚えて把握することにより、お酒に詳しくなり自分の人生に幅を持たすことができると思っている
>
> ラベルを写真をとって保存することもしたいし、メニューなどから検索しても保存できるようにしたい
>
> ただ保存するだけでは面白くないので、お酒の名前、品種、地産、どういう味わいか、蘊蓄な特徴などの項目を設けたい

同日の補足（質問への回答）：

> 過去に撮り溜めた写真の取り込みもしたい

> ログインに意味を持たせたい、複数ユーザーはどちらでも

> よく写真を取るのは、ワインと日本酒だからその2個で

殴り書きに先行してチャットで出ていた要望：

> ログイン機能も持たせるから、ログインする意味があるものがいいし、もし記録するやつだったら、自動で入力される奴がいいよね(写真読み取ったら、AIがそれを調べて表示して、入力もできるような)

> ラベル記録ってあるけど、ラベル以外でも検索したい。例えばお酒の名前がメニューにあってそれをその場で入れたらいろんな情報が出るような。写真以外で、メニューや品種、特徴からもでれたらいいね

質問リスト一巡後の追加要望（2026-08-20）：

> 今回はログインなしでも利用できるようにする。ただし保存機能はログインしないと扱えないので、ログイン無しの場合は、その場で調べることができるみたいなニュアンス

> メニューから調べた場合、画像も引っ張って来れるようにした、もちろん見つかなければ空欄でもいいが、自分でアップロードもできるようにしたい

<a name="nouns-verbs"></a>

## 名詞と動詞（段階 4・Claude 起案。2026-08-21）

拾う対象は確定した 01（要旨・前提・業務フロー・例外）。**拾うだけで捨てない**。仕分けは段階 5。

### 動詞 ── 状態を変える（書き込む。行き先は日本語の候補名）

| 主語 | 動詞（能動形） | 行き先の候補 |
|---|---|---|
| 記録者 | アカウントを登録する | アカウント |
| 記録者 | ログインする | （セッション。モデルにするかは段階 5） |
| 記録者 | ラベルの写真を撮って登録する | 写真、飲んだ記録（未確定） |
| 記録者 | 調べた酒を「飲んだ記録」として保存する | 飲んだ記録 |
| 記録者 | 飲まなかった酒を「気になる」として残す | 気になる |
| 記録者 | 「気になる」の酒を「飲んだ記録」に昇格させる | 気になる → 飲んだ記録 |
| 記録者 | AI が調べた欄（味わい・豆知識など）を編集する | 銘柄情報の欄 |
| 記録者 | 備考欄に感想を書く | 備考 |
| 記録者 | 記録を確定する | 記録の状態（未確定 → 確定） |
| 記録者 | AI に調べ直させる（再照会する） | 銘柄情報の欄（上書き） |
| 記録者 | 手入力で登録する | 飲んだ記録 |
| 記録者 | 写真をアップロードして記録に付ける | 写真 |
| 記録者 | あげていなかった写真をまとめてアップロードする | 写真、飲んだ記録（未確定） |
| 記録者 | 記録・写真を個別に削除する | 飲んだ記録、写真 |
| 記録者 | ゲストで調べた結果を、ログイン後に引き継いで保存する | 照会結果（一時） → 飲んだ記録 |
| システム | 知識源の推定結果を「未確定」として記録に書き込む | 飲んだ記録（未確定） |

### 動詞 ── 値を返す（読むだけ。モデルは増えない）

- ゲスト／記録者が、写真またはテキストで酒を**調べる**（結果を見る）
- 記録者が、銘柄情報と画像を**見る**（飲みながらチェックする）
- 記録者が、品種・産地・味わいで記録を**検索する**
- 記録者が、記録の一覧を**振り返る**
- システムが、写真・テキストを知識源に**照会する**（外部呼び出し）
- システムが、読み取れなかった旨を**示す**（手入力への切り替え案内）
- システムが、未確定の**印を表示する**
- 知識源が、銘柄情報を**推定して返す**／銘柄の画像を**探して返す**

### 動詞の形をしているが操作でないもの

- 「忘れてしまう」「知識になる」「覚えて把握する」「詳しくなる」「人生に幅を持たせる」──課題と目的の記述。操作ではない
- 「（保存機能を）扱える／扱えない」──権限の記述。例外・ルール側で拾済み

### 名詞（拾うだけ。仕分けは段階 5）

お酒／ワイン／日本酒／ウイスキー／種類／銘柄／銘柄情報／ボトル／ラベル／写真（自分が撮ったラベル写真）／過去写真（あげていなかった写真）／画像（知識源が探してきたもの）／画像の出所／メニュー／店（店名）／名前（銘柄名）／品種（例：メルロー）／産地／味わい／豆知識（蘊蓄）／備考／感想／飲んだ記録／「気になる」／未確定の記録／確定（記録の状態）／未確定の印／推定結果（AI の推定）／照会／照会結果（ゲストの一時的なもの）／知識源（AI・検索）／手入力／まとめ取り込み／アカウント／記録者（利用者）／ゲスト（未ログイン）／ログイン／セッション（引き継ぎの間だけ生きる置き場）／一覧／検索条件（品種・産地・味わい）／いつ（飲んだ日）／削除／保存期間（無期限）／複数本のボトル／切り出し／期限・催促（無いこと）／知識（目的の語）

<a name="sifting"></a>

## 名詞のふるい分け（段階 5・Claude 起案。2026-08-21）

4 分類（エンティティ／値オブジェクト／ただの属性／対象外）。**迷ったら少ないほうに倒す。** 表はエンティティと対象外だけ（属性まで載せると読めなくなるため）。

### エンティティ（と、その候補）

| 名詞 | 仕分け | 理由 |
|---|---|---|
| アカウント | **エンティティ** | 単体で探す（ログイン）。個別に増減する。この ID が全データに乗るアクセス境界の鍵 |
| 飲んだ記録 | **エンティティ** | 中核。個別に増減し、単体で探す（検索・一覧）。同じ銘柄の記録が 2 件あっても別物 |
| 「気になる」 | **飲んだ記録と同一モデルの状態違い（仮）** | 昇格（気になる → 飲んだ）が状態遷移で表せる。別モデルに分けると昇格が「削除＋新規作成」になり、調べた日や推定結果の連続性が切れる。「カードとクーポンは同じモデルの状態違いだった」と同じ形。段階 8 で確定 |
| 銘柄情報（名前・種類・品種・産地・味わい・豆知識） | **⚠ 論点（段階 7 の主論点）** | 独立エンティティ（共通マスタ）にするなら、01 例外の「銘柄の情報は共通」を構造で保証できるが、**同定（2 回目に飲んだとき同じ銘柄だと誰がどう判定するか）と表記ゆれを引き受ける**。記録の属性群として持つなら同定は不要（共通は「同じ値が入る」という結果になる）。少ないほうに倒すなら属性群だが、01 の文言と張るので発注者と決める |
| 写真 | **属性に倒す（仮）** | 記録 1 件に写真 1 枚の想定（2 枚目以降が要る場面が 01 に無い）。「写真だけ削除」は属性を空にする操作と読める。複数枚が必要になったら戻す |

### 値オブジェクト・区分値・属性（所属先ごとに）

- **区分値**：種類（ワイン／日本酒／その他）、記録の状態（気になる・未確定・確定──形は段階 8）、画像の出所（自分が撮った／知識源が取得／なし）
- **飲んだ記録の属性**：名前・品種・産地・味わい・豆知識・備考・店・飲んだ日・画像（参照＋出所）
- **品種は文字列の属性に倒す（仮）**：マスタ化すると表記ゆれ（ピノノワール／ピノ・ノワール）と同定を引き受ける。検索（Q5）は文字列でも成立する。付録B 候補

### 対象外

| 名詞 | 理由 |
|---|---|
| 知識源（AI・検索） | 外部アクター。所持物なし。結果を写す列だけがモデルに残る |
| 照会結果（ゲストの一時） | 保存されない。ログインまで生かす置き場は実装（セッション等）で扱う |
| メニュー／ボトル／ラベル／複数本のボトル | 読む・写す対象の実世界の物。モデルは記録と写真（属性）で足りる |
| 推定結果／未確定の印／確定 | 独立の物ではなく、記録の状態と欄で表せる |
| 照会／手入力／まとめ取り込み／削除／ログイン／一覧／検索条件／切り出し | 操作。読むだけか、既存モデルへの書き込み |
| ゲスト／記録者 | アクター。モデルとしてはアカウント |
| 酒／ワイン／日本酒／ウイスキー／メルロー | 区分値・属性に入る「値」の例 |
| セッション | 実装の道具 |
| 過去写真 | 「写真」と同じもの（撮った時期が違うだけ） |
| 知識／期限・催促／保存期間 | 目的の語／「無いこと」の確認／前提 |

### ふるい分けの検算

- 対象外は 10 行 ── 0 件ではないので仕分けは機能している
- エンティティ確定は **2 つ（アカウント・飲んだ記録）**、仮が 2 つ（気になる＝状態、写真＝属性）、論点が 1 つ（銘柄情報）
- ウイスキーが名詞に残っているのは 01 の「今回やらない」の記述から拾ったもの。区分値には入れない（今回はワイン／日本酒／その他）

### ユーザーレビューでの修正（2026-08-21）

- **アカウントパスワードとセッションをエンティティに追加**（指摘：ユーザー）。根拠は雛形 `model_templates.md` の `CustomerPassword` / `CustomerSession`──パスワードはハッシュを本体から分離した別エンティティ、セッションはサーバ側で保持するログインセッションのモデル。**「セッション＝実装の道具で対象外」とした行は撤回**（雛形が正）。Claude が外した理由：雛形を見ずに一般論で仕分けた
- ただし**ゲスト照会結果の一時置き場はログインセッションとは別物**。未ログインの間の置き場なので、扱いは引き続き実装側（またはセッションを流用するかを段階 8 で決める）
- **銘柄情報は独立エンティティで確定**（発注者判断）。同定の方式（Q17）と銘柄の境界（Q16）は質問リストへ

これでエンティティは **アカウント／アカウントパスワード／セッション／飲んだ記録／銘柄** の 5 つ（＋気になる＝記録の状態違い・仮）。

### Q16・Q17 の確定と、管理者の整理（2026-08-21）

- **Q16：銘柄はユーザーごとに持つ**（発注者の理由：全銘柄をカバーできるわけがなく、本部管理型は途方もないコスト）。銘柄テーブルに accountId が乗る（境界の鍵）。候補提示は自分の銘柄の中だけから。編集・再照会・削除は自分の自由で、Q9 の「上書きで問題ない」と整合する。**共通マスタ化は「みんなで銘柄 DB を育てるアプリ」に進化するときの再検討事項** → 付録B「判断が変わる条件」候補
- **Q17：同定は候補提示＋記録者が選ぶ**（自動同定・外部コードは不採用。アプリの背骨「AI が下書きし、人が確定する」と同じ型）。**代償：重複銘柄はあり得る。統合機能は今回やらない** → 付録B 候補
- **管理者は 2 層に分けて整理した。** ①ドメインの管理者（共通マスタの裁定役。エンティティが増える）は Q16 の決定で不要に。②システム管理者（ユーザー削除・凍結などの運用）は**今回やらない** → 付録A 候補。理由：運用者＝自分で DB を直接触れる規模／role 列は既存全員を IS_USER にすれば後から足せる（accountId のような「後から足せない列」ではない）。判断が変わる条件：他人に配って問い合わせ・凍結対応が発生し始めたら

<a name="naming"></a>

## ユビキタス言語（段階 6・Claude 起案。2026-08-21）

クラス名まで。テーブル名の接頭辞は段階 7（コンテキスト）の後。

### 候補と 7 軸の評価 ── 銘柄

前提の構造：アカウント `1:*` 銘柄、銘柄 `1:*` 記録。

| 候補 | ①役割差 | ②意味 | ③並び | ④通じるか | ⑤長さ | ⑥拡張 | ⑦多重度 | 判定 |
|---|---|---|---|---|---|---|---|---|
| **Brand** | 9：記録との差が明確 | 8：酒の「銘柄」の定訳 | 8 | 9：「銘柄」とそのまま会話できる | 10 | 9：種類が増えても壊れない | 問題なし | **推奨** |
| Label | 8 | **2：ラベル写真と正面衝突。**「ラベルを消す」が二義になる | 8 | 6 | 10 | 7 | ─ | 却下 |
| Bottle | 6 | **4：物理の 1 本と読める** | 7 | 7 | 10 | 6 | **記録と 1:1 に誤読させる** | 却下 |
| Product | 7 | 6：一般的すぎる | 7 | **4：業務で「プロダクト」とは呼ばない** | 9 | 8 | ─ | 却下 |
| Liquor | 7 | **5：蒸留酒の含意が強く、ワインを外れて読める** | 7 | 6 | 10 | 6 | ─ | 却下 |

### 候補と 7 軸の評価 ── 記録

**名前を難しくしている構造**：このエンティティは「飲んだ」だけでなく**「気になる」（まだ飲んでいない）も同じモデルの状態違い**として含む（段階 5 の仮判断）。「飲んだ」を名前に焼き込むと、気になる行で名前が嘘になる。

| 候補 | ①役割差 | ②意味 | ③並び | ④通じるか | ⑤長さ | ⑥拡張 | ⑦多重度 | 判定 |
|---|---|---|---|---|---|---|---|---|
| **DrinkRecord** | 8 | 8：**Drink を名詞（飲み物）として読む**。「酒の記録」であり、気になる状態も嘘にならない | 8 | 8：「記録」とそのまま会話できる | 9 | 8 | 問題なし | **推奨** |
| TastingNote | 7 | 7：ワイン文化では自然 | 6：Brand と離れ、Note 系の別物に見える | 7 | 8 | **4：tasting＝飲んだ後の含意。気になる状態と矛盾** | ─ | 却下 |
| DrinkLog | 7 | 6：log は時系列の羅列の含意。1 件ずつ育てる（確定する）ものと合わない | 8 | 6：「ログ」は開発者の言葉 | 9 | 6 | ─ | 却下 |
| DrinkEntry | 6：entry が何の入口か読めない | 5 | 8 | 5 | 9 | 7 | ─ | 却下 |
| Record 単独 | **3：何の記録か読めない** | 5：SQL の語感とも被る | 5 | 7 | 10 | 5 | ─ | 却下 |

### アカウント系（雛形 `Customer`／`CustomerPassword`／`CustomerSession` の型を踏襲）

| 候補 | 評価 | 判定 |
|---|---|---|
| **Account / AccountPassword / AccountSession** | 接頭辞がそろい（③）、雛形と同じ分け方。中立で、記録者・ゲストのどちらの文脈でも嘘にならない | **推奨** |
| User / UserPassword / UserSession | `user` は PostgreSQL の予約語（②）。一般的すぎて役割が読めない（①） | 却下 |
| Recorder 系 | 記録者の訳としては正しいが、④「アカウント」と呼んでいる日常会話とズレる。録音機にも読める（②） | 却下 |

### 名前順に並べて確認（クラス名・仮）

```
Account
AccountPassword
AccountSession
Brand
DrinkRecord
```

Account 系 3 つが隣接（③）。Brand と DrinkRecord は独立概念なので離れていてよい。接頭辞の重なりなし。

### ユビキタス言語（候補。最終決定は発注者）

| 日本語 | 英語名 | 意味 | 多重度・備考 |
|---|---|---|---|
| アカウント | Account | 利用者。ログインの主体 | 全データの境界の鍵 |
| アカウントパスワード | AccountPassword | パスワードのハッシュ。本体から分離 | Account `1:1` |
| セッション | AccountSession | サーバ側で保持するログインセッション | Account `1:*` |
| 銘柄 | Brand | 酒の銘柄。AI の推定から育つ、ユーザーごとの一覧 | Account `1:*`、Brand `1:*` DrinkRecord |
| 記録 | DrinkRecord | 酒との記録。「気になる」「未確定」「確定」を状態として含む | Account `1:*` |

### 何と呼ばないか（言い換え禁止）

| 呼ばない | 理由 |
|---|---|
| 銘柄マスタ | マスタと呼ぶと共通・本部管理に読める。Q16 でユーザーごとと決めた |
| ラベル | 写真の意味でだけ使う。銘柄の意味では使わない |
| お気に入り／ブックマーク／ウィッシュリスト | 「気になる」と言い換えない。1 つの概念に 1 つの語 |
| ログ | 記録の言い換えにしない（開発の語） |
| テイスティングノート | 備考・味わいの言い換えにしない |

### ユーザーの指摘による再評価（2026-08-21）── User 却下の根拠が誤りだった

**指摘：** カリキュラムは `User` を使っている。それはいいのか？

**事実確認の結果、Claude の却下理由が二重に間違っていた。**

1. このプロジェクトの DB は **MySQL 8.0**（docker-compose で確認）。「user は PostgreSQL の予約語」は**このスタックに当てはまらない**。MySQL では user をテーブル名に使える
2. カリキュラム（`curriculum_02`）には **`User` / `UserPassword` / `UserSession`（テーブル `udb_user` 系）が実在する**。テーブル名には必ずコンテキストの接頭辞が付くので、裸の `user` テーブルはそもそも発生しない

**Claude が間違えた理由：** スタックと雛形を確認せず、一般知識（PostgreSQL の予約語）で採点した。「推測で答えられてしまう問いほど、資料に理由が書かれている」をまた踏んだ。

**再評価（Account 系 vs User 系）**

| | User 系 | Account 系 |
|---|---|---|
| 雛形との一致 | ◎ `User`/`UserPassword`/`UserSession` をほぼコピーできる（実装が楽） | 型（3 分割）は同じ。名前だけ違う |
| ④業務の言葉 | 発注者（自分）は要求文で「**アカウント**登録」「ログイン」と言っている | ◎ 発注者の言葉と一致 |
| ②予約語 | MySQL では問題なし（前提の訂正） | 問題なし（ACCOUNT も MySQL で使用可） |

決定打が ④（発注者の言葉）か雛形準拠かの選択になるため、**発注者に差し戻して再決定**。

<a name="contexts"></a>

## 概念を配る（段階 7・Claude 起案。2026-08-21）

**用語の最終確定（発注者）：`User` / `UserPassword` / `UserSession`**（理由：雛形準拠＋字数が少ない）。`Brand` / `DrinkRecord` は前回どおり確定。

### コンテキストの判断

**判断：コンテキストは 2 つ。`udb`（会員＝雛形に実装済み）と `drink`（酒の記録＝今回作る）。**

**理由：** `curriculum_02` のコンテキスト判断軸（変更の理由／ライフサイクル／管理する人／画面）で切ると、認証（変更の理由＝認証方式、画面＝設定）と記録（変更の理由＝記録の付け方、画面＝記録・一覧・検索）は触る場面が違う。カリキュラム自身も会員を `udb` として業務（sales 等）から分けている。さらに **`udb` は雛形に実装済みで、「既存の命名は勝手に変えない」** ── 名前ごと流用するのが最小コスト。

**却下した案：** 1 コンテキスト案（当初の下書き。`edu.customer` に全部同居する例をなぞった）。雛形の実物が udb を分けており、User 系を「既存で足りる」にできる利点が勝つため却下。

**依存の向き：** `drink → udb` の一方向（`DrinkRecord` と `Brand` が `User.Id` を持つ）。カリキュラムの `sales → udb` と同じ形。

### 配る表

| 概念 | 消したら誰が困るか | 変更権限 | → 誰の | 追加 / 既存で足りる / 動かす |
|---|---|---|---|---|
| User | 記録者（ログイン不能・全データの鍵） | 記録者 | 記録者（udb） | **既存で足りる**（雛形に実装済み。流用） |
| UserPassword | 記録者（ログイン不能） | 記録者 | 記録者（udb） | **既存で足りる**（同上） |
| UserSession | 困る人なし（再ログインすればよい） | システムが発行・失効、記録者がログアウトで終える | 記録者（udb） | **既存で足りる**（同上） |
| Brand | 記録者（記録から銘柄情報が消える） | 記録者（AI の再照会も記録者の操作） | 記録者（drink） | **追加** |
| DrinkRecord | 記録者（記録そのもの） | 記録者（状態遷移はすべて記録者が起こす） | 記録者（drink） | **追加** |

**配れなかった行：なし。** 「AI が下書きし記録者が直す」Brand も、消えて困るのは記録者・変更権限も記録者なので割れない──知識源は持ち主ではなく下書きの供給源。

**既存で足りると確認したこと：** udb の 3 エンティティ（新しく作るのは drink の 2 つだけ）。ゲストと知識源の所持物は 0 のまま。知識源の推定結果は Brand・DrinkRecord の欄（写す列）で足りる。

### テーブル名（接頭辞確定）と ③並び の再採点

```
drink_brand
drink_record        ← DrinkRecord。エンティティ名がコンテキスト名（drink）で始まるので重ねない
udb_user
udb_user_password
udb_user_session
```

- マージ規則のおかげで `drink_drink_record` にならず、`drink_record` が「酒の記録」とそのまま読める
- コンテキストごとに全テーブルが隣接（③）。`drink_brand` と `drink_record` が並び、Q16（ユーザーごとの銘柄）は接頭辞ではなく `userId` 列が語る（⑦）

<a name="contexts-fix"></a>

## 段階 7 の修正：コンテキストは 1 つに戻す（2026-08-21・ユーザーの反証）

**指摘（ユーザー）：** udb と drink は両方とも同じユーザーが触り、変更権限も同じに見える。分ける理由があるのか。カリキュラムでも最終的にはアカウント系とカートが同じコンテキストだった。

**検証：** `model_templates.md` の冒頭に**明記されていた**──「教材の写しとはコンテキストの切り方が違う。教材は udb／…の 4 つだが、雛形は common／customer／shop の 3 つ。会員は udb.User ではなく customer.Customer。**雛形が新しいので、設計はこちらに合わせる**」。雛形の `edu.customer` は Customer／CustomerPassword／CustomerSession／Cart／CustomerCoupon／CustomerStampCard を**同居**させている。コンテキストの判断軸（触る人・変更権限）でも、認証と記録は同一人物・同一権限で分かれない。

**修正：コンテキストは `user` の 1 つ。** User／UserPassword／UserSession／Brand／DrinkRecord の全部がここに入る（雛形 customer の型）。段階 7 で udb／drink に分けた判断は**却下**──根拠にした `curriculum_02` は古い方の切り方で、Claude が model_templates の冒頭注記を読み飛ばした。

**テーブル名（確定し直し）**

```
user                 ← User。エンティティ名がコンテキスト名で始まるので重ねない（MySQL では予約語でなく使用可）
user_brand
user_drink_record
user_password
user_session
```

- 名前順で 5 つ全部が隣接（③ 満点）。`user_brand`／`user_drink_record` は「ユーザーごと」（Q16）を接頭辞が語る
- 依存の向きの議論（drink → udb）は消滅（単一コンテキスト内の参照になる）
- **判断が変わる条件：** アクターが増えたとき（管理者を立てる、共有・公開機能で「見る人」が分かれる）にコンテキストの分割を再検討
- 実装メモ：雛形コードの実物は `app-lib` の `edu.udb` と設計雛形の `customer` の両方があるので、流用時の名前合わせは実装フェーズで決める

<a name="model-shape"></a>

## モデルの形（段階 8・Claude 起案。2026-08-21）

形だけ。メソッドと検証は書かない。作法は雛形 `model_templates.md`（Customer / CustomerCoupon）に合わせた。パッケージのルート名は実装時に確定。

### 状態の設計 ── 2 つのエンティティに 2 状態ずつ割れた

当初は DrinkRecord に「気になる／未確定／確定」の 3 状態を積む想定だったが、**「未確定／確定」は銘柄の状態**（AI の推定を自分が確認したか）で、**「気になる／飲んだ」は記録の状態**（体験がどうなったか）と気づいた。混ぜると「気になるだが内容は確認済み」が表せない。雛形の教訓「UserCoupon が 5 状態だったのは役目が 2 つ混ざっていたから。分けたら 2＋3 になった」と同じ形。

```
Brand.state        IS_UNCONFIRMED（未確定） → IS_CONFIRMED（確定）
                   ※ AI の再照会で欄が上書きされたら IS_UNCONFIRMED に戻る（逆流 1 本）

DrinkRecord.state  IS_INTERESTED（気になる） → IS_DRUNK（飲んだ）＝昇格（Q7）
```

負の code（終わった状態）は両方とも無し──期限・失効・取消が要求に存在せず、削除は物理削除（Q15）のため。

### `Brand`（銘柄）

```scala
package sakelog.user.model

import ixias.core.model.*

/**
 * 銘柄: ユーザーごとに育つ、酒の銘柄情報。
 *
 * AI（知識源）が下書きし、ユーザーが確認・修正して確定する。
 * name〜trivia が「AI が書ける欄」。再照会するとこの 5 欄は上書きされる。
 */
import Brand.*
case class Brand(
  id:        Option[Id],                             // 管理Id
  userId:    User.Id,                                // ユーザーId（アクセス境界の鍵）
  kind:      Kind,                                   // 種類
  name:      String,                                 // 銘柄名
  variety:   Option[String],                         // 品種（kind が読み方を決める）
  region:    Option[String],                         // 産地
  taste:     Option[String],                         // 味わい
  trivia:    Option[String],                         // 豆知識
  state:     Status        = Status.IS_UNCONFIRMED,  // 確認状態
  updatedAt: LocalDateTime = Now,                    // データ更新日
  createdAt: LocalDateTime = Now                     // データ作成日
) extends EntityModel[Id]

/**
 * 銘柄: 付随する型と処理の定義
 */
object Brand:

  // --[ Type Aliases ]------------------------------------------------
  type Id         = Id.Repr
  type WithNoId   = Entity.WithNoId[Id, Brand]
  type EmbeddedId = Entity.EmbeddedId[Id, Brand]

  // --[ Opaque Values ]-----------------------------------------------
  object Id extends Entity.Id[Long]

  // --[ Value Objects ]-----------------------------------------------
  /**
   * 種類。深く扱うのはワインと日本酒（対象は Q の回答で確定）
   */
  enum Kind(val code: Short) extends EnumStatus[Short]:
    case IS_WINE  extends Kind(code = 1) // ワイン
    case IS_SAKE  extends Kind(code = 2) // 日本酒
    case IS_OTHER extends Kind(code = 3) // その他（浅く受ける）

  /**
   * 確認状態
   */
  enum Status(val code: Short) extends EnumStatus[Short]:
    case IS_UNCONFIRMED extends Status(code = 1) // 未確定（AI の推定のまま）
    case IS_CONFIRMED   extends Status(code = 2) // 確定（ユーザーが確認した）
```

**ここで型が語っていること**

- `name` だけ必須──銘柄は名前さえあれば立つ（手入力の最小形）。品種・産地・味わい・豆知識は AI が埋められなければ空でよい
- `userId` が必須──ユーザーに属さない銘柄は存在しない（Q16）

**型では守れない決めごと**

- **`kind` が `variety` の読み方を決める**：IS_WINE ならぶどう品種、IS_SAKE なら米の品種など。IS_OTHER では原則使わない
- **AI（再照会）が上書きしてよいのは name / variety / region / taste / trivia の 5 欄だけ**。再照会したら `state` を IS_UNCONFIRMED に戻す（内容が自分の見ていないものに変わったため）
- **`(userId, name)` に一意制約は張らない**。重複銘柄は許容し、保存時の候補提示（Q17）で吸収する。統合機能は無い
- 記録が 0 件になっても銘柄は消さない（銘柄一覧そのものが「詳しくなる」ための資産）

**保存されるデータの例**

| id | userId | kind | name | variety | region | taste | trivia | state |
|---|---|---|---|---|---|---|---|---|
| 1 | 1 | IS_WINE | シャトー・メルシャン 桔梗ヶ原メルロー | メルロー | 長野県 塩尻 | 重すぎず滑らかなタンニン | 桔梗ヶ原は日本メルローの聖地と呼ばれる | IS_CONFIRMED |
| 2 | 1 | IS_SAKE | 風の森 ALPHA 1 | 秋津穂 | 奈良県 御所 | 微発泡で甘やか | 全量無濾過無加水生酒の蔵 | IS_UNCONFIRMED |
| 3 | 1 | IS_OTHER | 私の白猫（果実酒） | | | 甘口 | | IS_CONFIRMED |

### `DrinkRecord`（記録）

```scala
package sakelog.user.model

import ixias.core.model.*

/**
 * 記録: 酒との出会い 1 回分。
 *
 * 「気になる」（調べたが飲んでいない）も同じモデルの状態違い。
 * 写真は記録に付く（同じ銘柄でも飲むたびに撮る写真は別）。
 */
import DrinkRecord.*
case class DrinkRecord(
  id:          Option[Id],                           // 管理Id
  userId:      User.Id,                              // ユーザーId（アクセス境界の鍵）
  brandId:     Brand.Id,                             // 銘柄Id
  state:       Status,                               // 記録状態
  drankAt:     Option[LocalDate],                    // 飲んだ日（IS_DRUNK で埋まる。不明なら空）
  shopName:    Option[String],                       // 店名（どこで）
  memo:        Option[String],                       // 備考（自分の感想）
  photoUrl:    Option[String],                       // 写真
  photoSource: Option[PhotoSource],                  // 写真の出所
  updatedAt:   LocalDateTime = Now,                  // データ更新日
  createdAt:   LocalDateTime = Now                   // データ作成日
) extends EntityModel[Id]

/**
 * 記録: 付随する型と処理の定義
 */
object DrinkRecord:

  // --[ Type Aliases ]------------------------------------------------
  type Id         = Id.Repr
  type WithNoId   = Entity.WithNoId[Id, DrinkRecord]
  type EmbeddedId = Entity.EmbeddedId[Id, DrinkRecord]

  // --[ Opaque Values ]-----------------------------------------------
  object Id extends Entity.Id[Long]

  // --[ Value Objects ]-----------------------------------------------
  /**
   * 記録状態
   */
  enum Status(val code: Short) extends EnumStatus[Short]:
    case IS_INTERESTED extends Status(code = 1) // 気になる（調べたが飲んでいない）
    case IS_DRUNK      extends Status(code = 2) // 飲んだ

  /**
   * 写真の出所
   */
  enum PhotoSource(val code: Short) extends EnumStatus[Short]:
    case IS_SELF  extends PhotoSource(code = 1) // 自分が撮った
    case IS_FOUND extends PhotoSource(code = 2) // 知識源が探してきた
```

**ここで型が語っていること**

- `brandId` が必須──銘柄の無い記録は存在しない。手入力でも銘柄（name だけの Brand）を先に作る
- `drankAt` が `Option`──気になる（飲んでいない）と、取り込みで日付が分からない場合の両方を受ける
- 「なし」の写真は `photoUrl = None` で表す（出所の 3 通り目は行として持たない）

**型では守れない決めごと**

- **`state` が `drankAt` の読み方を決める**：IS_INTERESTED のとき `drankAt` は必ず None。IS_DRUNK では原則埋めるが、取り込み等で不明なら None を許す
- **`photoUrl` と `photoSource` は両方 Some か両方 None**（片方だけは不正）
- **`userId` は `brandId` の指す Brand の userId と一致**していなければならない（他人の銘柄への参照は境界破り）
- 昇格（IS_INTERESTED → IS_DRUNK）の逆方向は無い
- AI・再照会が触れるのは Brand 側だけ。`memo` と写真に AI は触れない──これは決めごとではなく**分割の構造が守っている**（Q9 の「備考は上書きされない」を型で実現）

**保存されるデータの例**

| id | userId | brandId | state | drankAt | shopName | memo | photoUrl | photoSource |
|---|---|---|---|---|---|---|---|---|
| 1 | 1 | 1 | IS_DRUNK | 2026-08-15 | ビストロ青山 | 彼女と。また頼みたい | /p/a1.jpg | IS_SELF |
| 2 | 1 | 2 | IS_INTERESTED | | 鮨わたなべ | | /p/b2.jpg | IS_FOUND |
| 3 | 1 | 1 | IS_DRUNK | | | | /p/c3.jpg | IS_SELF |
| 4 | 1 | 3 | IS_DRUNK | 2026-07-01 | | 手入力で登録 | | |

（3 行目＝まとめ取り込み直後：日付不明・銘柄は同じシャトー・メルシャン＝ brandId 共有。4 行目＝手入力・写真なし）

### 段階 8 で決めた判断（発注者レビュー待ち）

1. **状態を Brand（未確定/確定）と DrinkRecord（気になる/飲んだ）に分けた**
2. **写真は記録に置く**（同じ銘柄を 2 回飲めば写真も 2 枚。銘柄の代表画像は最新の記録から計算できるので保存しない）
3. **ヴィンテージ（年）は持たない**──01 の項目（名前・品種・産地・味わい・蘊蓄・備考）に無い。要るなら name に含めるか、判断が変わる条件として付録B へ
4. **記録が 0 件でも銘柄は残す**
5. **再照会したら Brand は未確定に戻る**

### 段階 8 のレビュー反映（2026-08-21・全 5 件確定）

| # | 判断 | 結果 |
|---|---|---|
| 1 | 状態の 2＋2 分割（Brand: 未確定/確定、DrinkRecord: 気になる/飲んだ） | **承認** |
| 2 | 写真は記録に置く | **確定**。銘柄側に置く利点は表示の速さだけで、それは「最新の記録の写真」を計算すれば賄える。写真は体験の一部（同じ銘柄でも飲むたび別の 1 枚）。記録の削除（Q15）と一緒に消えるのも素直 |
| 3 | ヴィンテージ（年）は持たない | **確定**。発注者の理由：普段行く店では何年ものかを気にしない（気にするのは高級店くらい）。→ 付録B 候補（判断が変わる条件：年で選ぶ飲み方を始めたら） |
| 4 | 記録が 0 件でも銘柄は残す | **確定**。記録越しにしか銘柄は見えないので、残した銘柄が現れるのは候補提示（Q17）だけ。「記録は消しても銘柄の知識は財産」という意味になる。消す案は消し処理が増えるだけで利点が無い |
| 5 | 再照会したら Brand は未確定に戻る | **承認** |

**レビュー中に見つかった修正・確認 2 件**

- **修正：AI（再照会）が上書きできる欄は `kind` を含む 6 欄**（kind / name / variety / region / taste / trivia）。起案の決めごとは「5 欄」と書いていたが、種類の判別も AI の仕事（構造化出力で WINE / SAKE / OTHER に縛って返させ、間違いは記録者が直す）。発注者の質問「enum の判別は Gemini がやるのか」で矛盾が発覚した。03 では 6 欄で書く
- **確認：品種・産地は種類中立**。kind が読み方を決める（ワイン＝ぶどう品種、日本酒＝米の品種。日本酒は非公表も多いので Option が効く）。**精米歩合・特定名称などの種類固有の列は持たない**──01 の項目に無く、深さは AI が味わい・豆知識に書く中身で出す。判断が変わる条件：精米歩合などで検索したくなったら列に昇格 → 付録B 候補

<a name="verification"></a>

## 流す（段階 9・Claude 実施。2026-08-21）

### 新機能フローの通し ── 各ステップで何が書かれるか

| フロー | モデルの動き |
|---|---|
| 写真を撮って登録（本文 3） | Brand を新規作成（AI の 6 欄・IS_UNCONFIRMED）＋ DrinkRecord 作成（IS_DRUNK・photoSource=IS_SELF・drankAt=当日） |
| テキストで調べる（本文 4） | **書き込みなし**（表示のみ）← 穴 A で確定 |
| 飲んだ記録／気になるとして保存（本文 6） | Brand 作成（または候補から既存に紐づけ）＋ DrinkRecord 作成（IS_DRUNK または IS_INTERESTED・photoSource=IS_FOUND か None） |
| 昇格（本文 7） | DrinkRecord.state → IS_DRUNK、drankAt を埋める |
| 確定（本文 8） | Brand の 6 欄を修正 → Brand.state → IS_CONFIRMED |
| 再照会（本文 9） | Brand の 6 欄上書き → IS_UNCONFIRMED に逆流 |
| まとめ取り込み（本文 11） | 写真ごとに Brand（IS_UNCONFIRMED）＋ DrinkRecord（IS_DRUNK・drankAt=None）。**重複 Brand ができ得るが、後の確認（本文 12）で既存 Brand に付け替えられる**（brandId の更新） |
| 検索（本文 13） | 読むだけ（Brand の variety / region / taste を文字列検索） |
| 削除（本文 14） | DrinkRecord を物理削除＋写真ファイル削除。Brand は残す（レビュー #4） |

### 見つかった穴と対応

| # | 穴 | 対応 |
|---|---|---|
| A | **「調べただけ・まだ振り分けてない」状態が存在しない。** 旧フロー 16 は「記録者の照会なら未確定として書き込む」だったが、テキストで調べた瞬間に書くなら、飲んだ／気になるのどちらでもない状態が要ることになり矛盾 | **照会は書き込まない**で確定。書き込みは保存操作（写真からの登録は保存を兼ねる＝Q4 の最小タップ、テキストは本文 6 の明示操作）。ゲストと記録者の「調べる」が同じ振る舞いになり、Q11 の引き継ぎとも整合。01 のフロー 15〜17 を修正 |
| B | **退会が存在しない。** DrinkRecord・Brand は User.Id を参照しているのに、User の「戻す操作」（退会）が起きたとき記録・銘柄・写真がどうなるか答えられない | **Q18 ★ へ**（設けるか。設けるなら全部消すのを提案） |
| C | **銘柄そのものを削除する操作が無い。** 記録 0 の銘柄は候補提示に出続ける（レビュー #4 で残すと決めた）が、候補を汚す銘柄を消したくなったときの手段が無い | **Q19 ★ へ**（今回やらない、の確認） |
| D | **例外の書き漏らし 2 行**（逆算チェックで発見）：drankAt・shopName に対応する例外が無い／取り込みの日付不明を許す記述が無い | 01 の例外に 2 行追記 |

### 既存フロー（User の戻す操作）

| 操作 | drink 側への波及 |
|---|---|
| 退会 | **穴 B。Q18 の回答待ち** |
| パスワード変更・リセット | 波及なし（udb 内で完結）── 確認した事実として記録 |
| セッション失効・ログアウト | 保存済みデータに波及なし。ゲストの一時照会結果はセッションと一緒に消えるが、それは仕様（保存されない） |

### 逆算チェック（例外 ↔ モデル）

- 例外 10 行中 9 行がモデルの属性・構造に対応：境界=userId／出所=photoSource／共通=brandId／未確定の印=Brand.state／無期限=期限列なし／手入力=name だけ必須／再照会=Brand と Record の分割、など
- **対応が無い 1 行：「1 枚の写真に複数本 → 1 本分」**。モデルではなく AI 読み取りの仕様なので、03 では「型では守れない決めごと」（運用ルール）側に置く。書きすぎではないと判断
- 逆方向（属性 → 例外）で漏れ 2 件を発見 → 穴 D として 01 に追記

### Q18・Q19 の確定（2026-08-21）── 段階 9 の穴 B・C を閉じた

- **Q18：退会は設けない**（発注者：必要になるシチュエーションが想定できない）。システム管理者と同じ整理──運用者＝自分が DB で消せる。機能ではなく**運用手順**として消す順序（記録 → 銘柄 → パスワード・セッション → ユーザー）を 03 の決めごとに書く。判断が変わる条件：他人に配って削除依頼が現実になったら → 付録B 候補
- **Q19：発注者の懸念（誤登録した銘柄が候補の精度を汚す）で提案を変更。「記録が 0 件の銘柄は削除できる」**。全銘柄を消せるようにすると参照中の記録の扱いという新しい穴が開くが、0 件限定なら構造的に開かない。誤登録の掃除は「記録を消す → 0 件になった銘柄を消す」で完結。**レビュー #4 は「勝手には消えない（デフォルト残す）＋掃除の手段はある」に補正**

<a name="final-check"></a>

## 出す前のチェック（2026-08-21・03 清書の前）

| 観点 | 結果 |
|---|---|
| ID で区別する理由（全エンティティ） | User=ログインの主体／UserPassword=本体と更新契機が違う・秘匿／UserSession=端末ごとに増減／Brand=同じ内容でもユーザーが違えば別物・記録から参照される／DrinkRecord=同じ銘柄でも回ごとに別 ── 全部答えられる |
| 値オブジェクトの理由 | Kind・Status・PhotoSource は値が同じなら同一。ID 不要 |
| 新エンティティ 5 個以内 | 新規 2（Brand・DrinkRecord）。User 系 3 つは既存で足りる |
| 名前・接頭辞・単数形 | user_* で 5 テーブル隣接。全部単数形 |
| コンテキスト | 1 つ（触る人が 1 人）。またぐ参照なし |

**チェックで見つけた整理 2 件**

1. **1 つのものに 2 つの名前が残っていた。**「写真」と「画像」（出所が違うだけで同じもの）、「豆知識」と「蘊蓄」、「備考」と「感想」。03 の用語表で **写真／豆知識／備考 に統一**し、言い換えを禁止する。あわせて「**照会**（知識源に聞く。保存しない）」と「**検索**（自分の記録を探す）」を呼び分ける
2. **手入力で作った銘柄の初期状態が未定義だった。** AI を使っていないのだから最初から IS_CONFIRMED（自分が書いた内容＝確認済み）で作る。03 の決めごとに書く

<a name="impl-notes"></a>

## 実現方式のメモ（設計の外だが記録しておく）

- 知識源は差し替え可能な外部サービスとして扱う。実装候補は Gemini API 無料枠（2026-08 時点で 1 日 1,500 回）から始め、精度不足なら検索 API（Brave / Tavily 等の無料枠）を直列に足す 2 段構成
- デモは AI 部分をモック（固定 JSON）にしても「推定 → 確認 → 確定」の流れは見せられる
- コストは月数十円オーダー。題材選定の変数にしない、と合意済み
- **知識源が探してきた画像の保存は権利面がグレー**（他人の商品写真）。個人利用の範囲では実害は小さいが、公開デプロイするなら画像 URL の参照に留めるか自分の写真だけにする選択肢を残す。実装時に判断
- **ログインなしの照会は API コストが不特定者に開放される**形になる。公開デプロイ時はレート制限などの乱用対策が要る。実装時の課題
- **品種・産地の表記ゆれ対策（モデルは文字列のまま）**：①入力・修正時に自分の過去の値をサジェストする（既存値から選べば表記が揃う）②AI への指示で表記を指定する（「品種は一般的なカタカナ表記で返せ」）。マスタ化は付録B の判断が変わる条件（2026-08-21 のユーザーの疑問から）
