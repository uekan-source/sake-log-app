# 作業の栞

**セッションの区切りに「どこまでやった」「次にやる」を上に追記する。** 思考の記録は `docs/domain/drinkRecord/02_analysis.md` に書く。ここは現在地だけを短く。

---

## 2026-08-24

**どこまでやった**

- **発注者レビュー（別 AI・発注者役）を受け、指摘 4 件を全件検証 → 全件反映**
  - Q22：銘柄の一覧を追加（フロー 13・14 の行内。番号は崩していない）
  - Q23：照会失敗の仮銘柄は `kind = IS_OTHER` で仮置き（二義の代償を決めごとに明記、データ例に行追加）
  - 追記 2 件：照会は結果をその場で返す（Q4）／写真だけ削除できる
- 質問リストは Q23 まで。作業の栞（このファイル）と CLAUDE.md の運用を新設

**次にやる**

- **ボスのレビュー回答の処理**（提出済み・回答待ち）。指摘と検証を分け、反証してから採用
- 理解度チェック 5 問（銘柄の独立／状態の 2+2／enum と文字列／写真経路の例外／userId の後から足せなさ）が未回答のまま残っている

## 2026-08-21 まで（`300db69`）

**どこまでやった**

- **設計フェーズ完了。** 手順書の 9 段階を完走し、ボスへレビュー提出済み（回答待ち）
  - 01：要求の要旨・前提 5 項目・質問リスト Q1〜Q21（全回答）・業務フロー 19 ステップ＋例外
  - 02：思考過程一式（段階 1〜9 のシート、判断の経緯、多視点レビューの記録。目次あり）
  - 03：提出用の設計書（用語 12 語／ER 図・状態遷移図＝手書き SVG／EntityModel 5 本＋3 点セット／区分値／またぐルール／付録A・B）
- **多視点レビュー（6 視点）を実施・反映済み。** 最重要は「写真経路に候補提示が無い → 付け替え操作を正式化」。3 ファイルの突き合わせで齟齬 6 件も修正済み
- **実装の骨格を研修リポジトリから流用済み。** 認証雛形（signup / login / logout / me）・ixiaS・SvelteKit の器・MySQL（docker-compose）・OpenAPI。パッケージは `edu.user.*` に読み替え、テーブルは `user` / `user_password` / `user_session`。**Brand.scala / DrinkRecord.scala も `edu/user/model/` に追加済み**（03 の写し。モデルのみ）

**次にやる**

1. **ボスのレビュー回答の処理**──指摘と検証を分け、反証してから採用。経緯は 02 に追記
2. レビュー通過後、**実装フェーズ開始**。初日：`docker compose up` → `sbt compile` → 認証雛形の動作確認（コンパイルは未確認のまま）
3. 縦 1 本＝照会 → 未確定 → 確定 → 一覧（写真は IS_SELF のみ）。着手前に決める順序は 02「多視点レビュー」節の実装引き継ぎを見る

**未処理・保留**

- Brand / DrinkRecord の persistence・table 定義・マイグレーション SQL（実装フェーズで）
- IS_FOUND（知識源が探す写真）は画像検索 API の 2 本目が必要。縦 1 本ではやらない
- 公開デプロイ時の課題（レート制限・写真配信の認可の実装）は 03 の決めごとと 02 のメモに記録済み
