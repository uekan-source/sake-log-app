#
# Copyright IxiaS, Inc. All Rights Reserved.
#
# For the full copyright and license information,
# please view the LICENSE file that was distributed with this source code.
#

openapi: '3.1.0'
info:
  title: Education Book API
  version: 1.0.0
  description: >-
    Training template API. Authentication is a signed `session` cookie issued
    by signup / login and resolved server-side against `udb_user_session` on
    every request.
  license:
    name: IxiaS
servers:
  - url: http://localhost:9000
    description: Local Development
paths:
  /ping:
    get:
      $ref: './paths/ping.yaml'
  /user/api/signup:
    post:
      $ref: './paths/user-signup.yaml'
  /user/api/login:
    post:
      $ref: './paths/user-login.yaml'
  /user/api/logout:
    post:
      $ref: './paths/user-logout.yaml'
  /user/api/me:
    get:
      $ref: './paths/user-me.yaml'
